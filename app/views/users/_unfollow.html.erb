<%= form_for(current_user.relationships.find_by_followed_id(@user),
             html: { method: :delete }) do |f| %>

 <%= button_tag(type: "submit", class: 'btn btn-danger dropdown-toggle' ) do %>
    <i class= "fa fa-user-times"></i> Unfollow 
    <% end %>
<% end %>